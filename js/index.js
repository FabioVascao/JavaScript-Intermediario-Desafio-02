import {
  buttonForest,
  buttonRain,
  buttonMarket,
  buttonFire,
  buttonPressForest,
  buttonPressRain,
  buttonPressMarket,
  buttonPressFire,
  timerEnd,
  buttonPlay,
  buttonStop,
  buttonUp,
  buttonDown,
  minutesDisplay,
  secondsDisplay,
  minutes,
  seconds
} from "./elements.js"

import Sounds from "./sounds.js"
import Timer from "./timer.js"

const sound = Sounds()
const timer = Timer()
